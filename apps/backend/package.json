{
  "name": "@plenor/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -r ./src/runtime/nextEnvCompat.cjs --import tsx --watch src/server.ts",
    "start": "node -r ./src/runtime/nextEnvCompat.cjs --import tsx src/server.ts",
    "typecheck": "tsc --noEmit --pretty false --project tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "pino-pretty": "^13.1.2",
    "tsx": "^4.20.6"
  }
}
